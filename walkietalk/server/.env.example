# Port the signaling server listens on (Railway sets this automatically).
PORT=3000

# Allowed CORS origin for Socket.io clients. Use * for development, or set to
# your app's origin in production.
CLIENT_ORIGIN=*
